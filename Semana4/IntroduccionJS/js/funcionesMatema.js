let resultado = 0;

resultado = Math.PI;
resultado = Math.round(resultado);
resultado = Math.ceil(8.8); // redondea hacia arriba
resultado = Math.floor(8.8); // redondea hacia abajo
resultado = Math.round(8.5); // busca el numero mas cercano
resultado = Math.sqrt(16); // raiz cuadrada
resultado = Math.abs(-8); // valor absoluto
resultado = Math.pow(8, 2); // potencia
resultado = Math.min(1, 2, 3, 4, 5); // minimo
resultado = Math.max(1, 2, 3, 4, 5); // maximo
resultado = Math.random(); // numero aleatorio entre 0.0 y 1(0.9999999999999999)
resultado = Math.floor(Math.random() * 20 + 1); // numero aleatorio entre 0 y 20
console.log(resultado);