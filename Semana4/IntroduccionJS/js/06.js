// Números
let resultado;

resultado = Math.PI;
resultado = Math.round(2.6); //Redondea al entero mas cercano 3
resultado = Math.ceil(2.6); //Redondea al entero mas cercano 3(hacia arriba)
resultado = Math.floor(2.6); //Redondea al entero mas cercano 2(hacia abajo)
resultado = Math.sqrt(4); //Raiz cuadrada
resultado = Math.abs(-4); //Valor absoluto
resultado = Math.pow(8, 2); //Potencia
resultado = Math.min(2, 3, 4, -5, 6); //Minimo
resultado = Math.max(2, 3, 4, -5, 6); //Maximo
resultado = Math.random(); //Numero aleatorio entre 0 y 1
resultado = Math.floor(Math.random() * 20 + 1); //Numero aleatorio entre 1 y 20